/* ==================== workout.js ====================
   Reserved for workout-tracking logic (exercise checklists,
   timers, logging a completed session, etc.).
   The "Today's Workout" hero widget and the workout demo
   screen are currently static markup/CSS only — there is no
   JS behavior to move here yet. Add real workout functions
   here as that feature gets built out. */
